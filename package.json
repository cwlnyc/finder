{
  "name": "finder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Watch NYC public records for newly registered/licensed businesses and turn them into a filtered daily feed.",
  "engines": { "node": ">=20" },
  "scripts": {
    "records": "node records/cli.mjs",
    "probe": "node records/cli.mjs probe --all",
    "test": "node --test records/*.test.mjs"
  }
}
